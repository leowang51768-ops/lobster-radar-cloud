name: Legacy Strategy Radar (disabled)

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archived workflow
        run: |
          echo "This legacy direct-LINE workflow is disabled."
          echo "Integrated signals now run through main.yml and buy_signal.py."
