name: Legacy 30MA Direct LINE (disabled)

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archived workflow
        run: |
          echo "This legacy direct-LINE workflow is disabled."
          echo "The 30MA key-candle route is integrated into buy_signal.py."
